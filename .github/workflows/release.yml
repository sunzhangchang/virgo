name: C/C++ CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: make
      run: make
    - name: zip
      run: zip
    - name: action-zip
      uses: montudor/action-zip@v1.0.0
      with:
        args: zip -q virgo.zip ./build/virgo.exe
    - name: release
      uses: softprops/action-gh-release@v1
      if: startsWith(github.ref, 'refs/tags/')
      with:
        files: virgo.zip

